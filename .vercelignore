# Trim the deploy upload — non-build docs only.
# NOTE: patterns are ROOT-ANCHORED with a leading "/" on purpose. An un-anchored
# "supabase/" would also match lib/supabase/ and break the build — don't do that.
/docs/
/DEPLOY.md
